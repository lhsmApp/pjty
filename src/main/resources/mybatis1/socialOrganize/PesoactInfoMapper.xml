<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="PesoactInfoMapper">
	
	<!--表名 -->
	<sql id="tableName">
		tb_pesoact_info
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		PESO_NAME,	
		ACT_NAME,	
		CAPI_SITU,	
		EFFECT,	
		REMARK
	</sql>
	<sql id="SelectField">
		PESO_NAME,	
		ACT_NAME,	
		CAPI_SITU,	
		EFFECT,	
		REMARK,	
		PESO_NAME as PESOACT_PESO_NAME,	
		ACT_NAME as PESOACT_ACT_NAME
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
		#{PESO_NAME},	
		#{ACT_NAME},	
		#{CAPI_SITU},	
		#{EFFECT},	
		#{REMARK}
	</sql>
	<sql id="SelectFieldValue">
		#{PESO_NAME},	
		#{ACT_NAME},	
		#{CAPI_SITU},	
		#{EFFECT},	
		#{REMARK},	
		#{PESO_NAME},	
		#{ACT_NAME}
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
	<include refid="Field"></include>
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			PESO_NAME = #{PESOACT_PESO_NAME}
		and 
		    ACT_NAME = #{PESOACT_ACT_NAME}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			PESO_NAME = #{PESO_NAME},
			ACT_NAME = #{ACT_NAME},
			CAPI_SITU = #{CAPI_SITU},
			EFFECT = #{EFFECT},
			REMARK = #{REMARK}
		where 
		    PESO_NAME = #{PESOACT_PESO_NAME}
		and 
		    ACT_NAME = #{PESOACT_ACT_NAME}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="SelectField"></include>
		from 
		<include refid="tableName"></include>
		where 
			PESO_NAME = #{PESOACT_PESO_NAME}
		and 
		    ACT_NAME = #{PESOACT_ACT_NAME}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="SelectField"></include>
		from 
		<include refid="tableName"></include>
		where 1=1
		<if test="pd.keywords!= null and pd.keywords != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
					 or 
					字段2 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%') 
				-->
				)
		</if>
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="SelectField"></include>
		from 
		<include refid="tableName"></include>
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
        <foreach collection="list" item="item" open="" close="" separator="OR">  
            (PESO_NAME = #{item.PESOACT_PESO_NAME} and ACT_NAME = #{item.PESOACT_ACT_NAME})
        </foreach> 
	</delete>
	
	<!-- fh313596790qq(青苔) -->
</mapper>