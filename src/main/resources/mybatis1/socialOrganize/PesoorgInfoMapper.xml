<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="PesoorgInfoMapper">
	
	<!--表名 -->
	<sql id="tableName">
		tb_pesoorg_info
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		PESO_NAME,	
		STAFF_JOB,	
		LEADER_NAME,	
		STAFF_SEX,	
		MOBILE_TEL,	
		REMARK
	</sql>
	<sql id="SelectField">
		PESO_NAME,	
		STAFF_JOB,	
		LEADER_NAME,	
		STAFF_SEX,	
		MOBILE_TEL,	
		REMARK,	
		PESO_NAME as PESOORG_PESO_NAME,
		STAFF_JOB as PESOORG_STAFF_JOB,
		LEADER_NAME as PESOORG_LEADER_NAME
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
		#{PESO_NAME},	
		#{STAFF_JOB},	
		#{LEADER_NAME},	
		#{STAFF_SEX},	
		#{MOBILE_TEL},	
		#{REMARK}
	</sql>
	<sql id="SelectFieldValue">
		#{PESO_NAME},	
		#{STAFF_JOB},	
		#{LEADER_NAME},	
		#{STAFF_SEX},	
		#{MOBILE_TEL},	
		#{REMARK},	
		#{PESO_NAME},	
		#{STAFF_JOB},	
		#{LEADER_NAME}
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
	<include refid="Field"></include>
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			PESO_NAME = #{PESOORG_PESO_NAME}
	    and
			STAFF_JOB = #{PESOORG_STAFF_JOB}
		and
			LEADER_NAME = #{PESOORG_LEADER_NAME}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			PESO_NAME = #{PESO_NAME},
			STAFF_JOB = #{STAFF_JOB},
			LEADER_NAME = #{LEADER_NAME},
			STAFF_SEX = #{STAFF_SEX},
			MOBILE_TEL = #{MOBILE_TEL},
			REMARK = #{REMARK}
		where 
			PESO_NAME = #{PESOORG_PESO_NAME}
	    and
			STAFF_JOB = #{PESOORG_STAFF_JOB}
		and
			LEADER_NAME = #{PESOORG_LEADER_NAME}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="SelectField"></include>
		from 
		<include refid="tableName"></include>
		where 
			PESO_NAME = #{PESOORG_PESO_NAME}
	    and
			STAFF_JOB = #{PESOORG_STAFF_JOB}
		and
			LEADER_NAME = #{PESOORG_LEADER_NAME}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="SelectField"></include>
		from 
		<include refid="tableName"></include>
		where 1=1
		<if test="pd.keywords!= null and pd.keywords != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
					 or 
					字段2 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%') 
				-->
				)
		</if>
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="SelectField"></include>
		from 
		<include refid="tableName"></include>
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			PESO_NAME in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	<!-- fh313596790qq(青苔) -->
</mapper>